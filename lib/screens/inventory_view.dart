// lib/screens/inventory_view.dart
import 'package:flutter/material.dart';

class WarehousesView extends StatelessWidget {
  const WarehousesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('المخزون')),
      body: const Center(
        child: Text('شاشة المخزون قيد التطوير'),
      ),
    );
  }
}
